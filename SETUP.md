# تشغيل النظام على mahmoudalfailakawi.site

ثلاث خطوات، مرة واحدة فقط. لا يوجد أي SQL يُشغَّل يدويًا — الجداول وحساب المالك
وبيانات العمارتين تُنشأ تلقائيًا عند أول فتح للموقع.

---

## ١) النشر — Vercel

١. [vercel.com/new](https://vercel.com/new) ← **Import** مستودع `ix1ali/MHAlfailkawe`.
٢. اترك الإعدادات كما هي (Next.js) واضغط **Deploy**.
   أول نشر سيعمل لكن صفحة الدخول ستقول إن قاعدة البيانات غير مربوطة — هذا طبيعي.

## ٢) قاعدة البيانات — Neon (مجانية)

١. داخل مشروع Vercel ← تبويب **Storage** ← **Create Database** ← **Neon (Serverless Postgres)**.
٢. الخطة: **Free**. المنطقة: **Frankfurt (eu-central-1)** أقرب منطقة للكويت.
٣. **Connect** بالمشروع لكل البيئات (Production / Preview / Development).
   يضيف Vercel المتغير `DATABASE_URL` تلقائيًا — لا تنسخ أي مفتاح بيدك.
٤. **Deployments** ← أحدث نشر ← **Redeploy**.

## ٣) الدومين — mahmoudalfailakawi.site

**Settings → Domains → Add** ← `mahmoudalfailakawi.site` (وأضف `www.mahmoudalfailakawi.site` يحوّل إليه).

إن كان الدومين من جهة غير Vercel، أضف عند مزوّد الدومين:

| النوع | الاسم | القيمة |
|---|---|---|
| `A` | `@` | `76.76.21.21` |
| `CNAME` | `www` | `cname.vercel-dns.com` |

(إن عرض Vercel قيمًا مختلفة فاعتمد ما يعرضه.)

---

## أول دخول

| | |
|---|---|
| **اسم المستخدم** | `Ali` (لا فرق بين الحروف الكبيرة والصغيرة) |
| **كلمة المرور** | `Aa112233@` |

ثم **الإعدادات ← حسابي ← كلمة المرور** وغيّرها فورًا.

---

## المستخدمون

**المستخدمون ← + مستخدم**:

| الدور | ماذا يستطيع |
|---|---|
| **مدير** | كل شيء |
| **مشرف عقار** | كل شيء داخل العمارة المسندة إليه فقط (حولي أو المنقف) |
| **محاسب** | اطلاع كامل بما فيه المالية، بلا تعديل |
| **حارس** | الشقق والمستأجرون والتنبيهات والمراسلات — بلا أي بيانات مالية |

---

## الحدود المجانية

- **Neon Free**: مساحة ٠٫٥ غيغا، وقاعدة البيانات «تنام» بعد ٥ دقائق بلا استخدام وتصحو
  تلقائيًا خلال ثانية عند أول طلب. النظام يتوقف عن سؤال الخادم بعد ١٥ دقيقة خمول
  فلا يستهلك الحصة وأنت بعيد.
- **المستندات** تُحفظ داخل قاعدة البيانات نفسها، والصور تُصغَّر تلقائيًا قبل الرفع
  (الحد ٤ ميغابايت للملف).

## نسخة احتياطية

**الإعدادات ← البيانات ← تصدير نسخة** — صدّر نسخة بين حين وآخر واحتفظ بها عندك.

---

## التطوير على جهازك

```bash
npm install
npm run db:local        # قاعدة Postgres محلية في مجلد .pglite (نافذة منفصلة)
cp .env.example .env.local   # ثم: DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5433/postgres
npm run dev
```
